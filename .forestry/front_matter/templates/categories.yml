---
label: Categories
hide_body: true
fields:
- name: categories
  type: field_group_list
  fields:
  - name: category
    type: include
    config: {}
    template: category
    label: Category
  config:
    min: 
    max: 
    labelField: name
  label: Categories
pages:
- Data/Categories.md
